# .env.example

# --- Core SSF / Okta ---
SSF_ISSUER=https://ssf.yourdomain.com
OKTA_ORG=https://your-org.okta.com

# --- Lookout OAuth ---
# Application key from Lookout (used to request access tokens)
LOOKOUT_APP_KEY=<REPLACE_WITH_LOOKOUT_APPLICATION_KEY>

# Optional: override token endpoint if needed
# LOOKOUT_TOKEN_URL=https://api.lookout.com/oauth2/token

# --- Lookout polling behavior ---
# How often to poll Lookout (in seconds)
LOOKOUT_POLL_INTERVAL_SECONDS=60

# How far back to look for updated devices on each poll (in minutes)
LOOKOUT_SINCE_MINUTES=5

# Optional: pin to a single Lookout tenant (enterprise GUID)
# LOOKOUT_ENTERPRISE_GUID=<REPLACE_WITH_LOOKOUT_ENTERPRISE_GUID>
